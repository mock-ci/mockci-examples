# Java example for DynamoDB

To run this example:
```bash
mvn clean compile exec:java
```

You can also input you API key in `src/main/java/Main.java` to run the example without the rate limits of anonymous sessions